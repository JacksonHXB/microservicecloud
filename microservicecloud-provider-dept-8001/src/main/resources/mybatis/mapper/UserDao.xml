<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hxb.dao.UserDao">
    
    <!-- 
    select user.id,user.username,dict.name as gender from tb_dict_item dict inner join tb_user user on dict.value = user.gender
     -->
     <!--  
    
	<select id="queryAllUsers" resultType="com.hxb.entity.User">
		SELECT * FROM tb_user
		<if test="keyword != null">
			WHERE username LIKE CONCAT('%',#{keyword},'%')
		</if>
		<if test="sort != null">
			ORDER BY ${sort} ASC	
		</if>
		<if test="size!= 0">
			LIMIT #{page}, #{size}
		</if>
	</select>
     -->
	
	<!-- 插入用户 -->
	<insert id="insertUser" parameterType="com.hxb.entity.User" >
		<selectKey resultType="java.lang.Integer" order="AFTER" keyProperty="id">
			SELECT LAST_INSERT_ID()
		</selectKey>
		INSERT INTO tb_user(username,password,gender,birth,email)values(#{username},#{password},#{gender},#{birth, jdbcType=TIMESTAMP},#{email})
    </insert>
    
    <!-- 删除用户 -->
	<delete id="deleteUser" parameterType="java.lang.Integer">
		DELETE FROM tb_user WHERE id=#{id}
	</delete>
	
	<!-- 更新用户 -->
	<update id="updateUser" parameterType="com.hxb.entity.User">
		UPDATE tb_user SET username=#{username}, password=#{password}, gender=#{gender},birth=#{birth, jdbcType=TIMESTAMP}, email=#{email} WHERE id=#{id}
	</update>
</mapper>



















































